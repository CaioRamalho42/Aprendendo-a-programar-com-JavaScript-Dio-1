const students = ['Bob', 'John', 'Eric', 'Janis']; /*Um array ou lista é uma coleção de dados direta.*/

students.push('Nicholas'); /*'.push' adiciona um dado ao array, na última posição.
É possível adicionar também usando o seguinte comando 'students [4] = 'Nicholas'', posição nos colchetes.*/

students.pop(); /*Remove o último dado do array. '.shift()' é o oposto, pega o primeiro dado.*/

console.log(students[0]); /*Lembrar que a primeira posição é 0.*/

for (let index = 0; index < array.length; index++) { /*For é a estrutura de repetição, 'let index = 0;' é a variável que irá dizer em qual posição começar a percorrer, 'index < array.length;' é a condição para executar a repetição, 'index++' mostra o que acontecerá depois de executar o for.*/
    
}